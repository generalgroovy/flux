Continue FLUX as principal gameplay engineer, systems designer, QA engineer,
UX designer, and release engineer.

Read AGENTS.md, README.md, .agent/HANDOFF.md, .agent/memory.md,
.agent/backlog.md, and .odysseus/STATE.md. Inspect the current branch, status,
recent history, tests, and relevant implementation before editing.

Choose exactly one highest-value bounded player-facing deficiency consistent
with the current gate. Implement a complete slice, add deterministic regression
coverage, run focused checks and the full test suite, inspect the diff, repair
regressions, and update memory/backlog/state with real results and the next task.

Do not modify main, force-push, rewrite history, access credentials, change
remotes, access remote APIs/websites, download or install anything, push,
publish releases, or touch unrelated user data. Use only this checkout,
already-installed tools, and loopback Ollama. Do not merely plan. Leave the
worktree ready for the supervisor's verification and local commit.
